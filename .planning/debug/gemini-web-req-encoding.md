---
status: resolved
trigger: "Continue the gemini-proxy reverse-engineering task: encode full request surface (tools, system, history, reasoning) into Gemini web frontend 97-slot inner_req_list"
created: "2026-07-31T01:10:00Z"
updated: "2026-07-31T01:10:00Z"
---

## Current Focus

hypothesis: "Gemini web frontend StreamGenerate 97-slot array only carries current-turn text, locale, attestation, category enum, and a few constants. System/history/tools are not encoded as separate slots in the live captures; the server tolerates empty attestation (slots 3/4). Therefore the proxy should flatten system, multi-turn history, and tool declarations into the prompt text, and use category_enum/model selection for reasoning/thinking."
test: "Run cargo test, cargo clippy, and live curls against the proxy for simple/system/multi-turn/tool/thinking cases."
expecting: "All checks pass; live requests return HTTP 200 and meaningful responses; streaming still works."
next_action: "Session complete — report results to user."

## Symptoms

expected: "Cookie-auth mode supports full OpenAI/Anthropic request surface: tools, system/developer instructions, multi-turn history, reasoning/thinking."
actual: "Request side flattens to prompt text + category enum; system instruction merged into user prompt as fallback; tools/history/reasoning not encoded into web frontend slots."
errors: []
reproduction: "Run curl against 127.0.0.1:3001 with system message, tools, multi-turn history, or thinking config; observe Gemini does not receive them as structured fields."
started: "Current commit e06be37; response parser fixed, request encoding incomplete."

## Eliminated

- hypothesis: "System instruction, history, tools, and thinking config are encoded in distinct slots of the 97-slot inner_req_list."
  evidence: "Live captures of simple/system/multi-turn/tool/thinking scenarios all have only slot 0 (prompt text) varying; other non-constant slots are attestation (3/4), request UUID (59), and category enum (30). The web frontend maintains history server-side via conversationId/Iq/J2, not in the array."
  timestamp: "2026-07-31T01:40:00Z"

- hypothesis: "Slots 3/4 attestation must be a valid server-signed token or requests will be rejected."
  evidence: "Current Rust code sends empty strings for slots 3/4 and receives HTTP 200; live captures show these are generated by a WebChannel snapshot service (_.ggd) that hashes textQuery+random UUID and signs it, which we cannot reproduce without the signing key. Empty values are accepted by the server in practice."
  timestamp: "2026-07-31T01:45:00Z"

## Evidence

- timestamp: "2026-07-31T01:10:00Z"
  checked: "repo state and .env"
  found: "main at e06be37, .env contains GEMINI_COOKIES, active debug sessions exist"
  implication: "Can use cookie-auth for live captures immediately."

- timestamp: "2026-07-31T01:40:00Z"
  checked: "/tmp/opencode/captures/*_req_*.json (pre-existing live captures)"
  found: "All StreamGenerate payloads are 97-slot arrays; only slot 0 (prompt text) changes across simple/system/multi-turn/tool/thinking scenarios. Slot 30 is always [4] (AUTO category)."
  implication: "Structured fields are not placed in separate slots in these captures; they are either server-side or merged into text."

- timestamp: "2026-07-31T01:45:00Z"
  checked: "BardChatUi_modules.js around _.ggd and _.Ncd"
  found: "_.ggd builds slots 3/4 by SHA-256(textQuery||random16bytes) then calling a snapshot service; _.Ncd sets protobuf fields 10/80/81/82/96 for tools/thinking but the 97-slot JSON is just field_number->slot_index."
  implication: "We cannot reproduce signed attestation; we can encode tools/thinking if we know protobuf shapes, but safest path is flattening into prompt and selecting model category for thinking."

- timestamp: "2026-07-31T01:50:00Z"
  checked: "src/openai/converter.rs and src/anthropic/converter.rs"
  found: "Converters already produce GenerateContentRequest with system_instruction, tools, thinking_config, and multi-turn contents; web frontend ignores these fields."
  implication: "Fix belongs in web_frontend.rs builder, not converters."

## Resolution

root_cause: "Gemini web frontend's StreamGenerate 97-slot payload is a single-turn protobuf; structured request features (system, history, tools, thinking) are either maintained server-side by conversation tokens or must be serialized into the prompt text. The proxy's cookie-auth path was not doing that serialization, so the model only saw raw user text."
fix: "Update build_inner_req_list to serialize system instruction, prior user/model turns, tool declarations, and thinking hints into the prompt text; keep category enum for model/thinking selection; update streaming servers to emit tool_call and reasoning deltas."
verification: "cargo test passes (46 unit + 10 integration), cargo clippy -- -D warnings passes, live curls return 200 for simple/system/multi-turn/tool/thinking requests, streaming simple chat works."
files_changed:
  - src/gemini/web_frontend.rs
  - src/openai/converter.rs
  - src/openai/server.rs
  - src/anthropic/converter.rs
  - src/anthropic/server.rs
